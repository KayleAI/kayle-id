/**
Screen — Passport photo page capture for the MRZ.

Title
Scan your passport

Body
We need to scan the photo page of your passport to prepare for the NFC check.

Please:

Open your passport to the photo page

Make sure the page is fully visible

Keep the text clear and in focus

This scan is used only to unlock the passport’s NFC chip and is not stored.

Primary button
Scan passport

Secondary button
Back

Optional helper (small text, auto-dismiss)
Make sure all four corners of the passport's photo are visible.
 */

/**
 * This component is used to capture the passport's photo page using a camera.
 */
export function SessionPassportCapture() {
  return (
    // TODO: Implement the passport photo page capture flow.
    null
  );
}
