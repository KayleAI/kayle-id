{
  "name": "kayle-id",
  "description": "Privacy-first identity verification.",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --filter=api,web",
    "dev:ios": "turbo run dev --filter=ios",
    "dev:android": "turbo run dev --filter=android",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.7",
    "husky": "^9.1.7",
    "turbo": "^2.6.1",
    "ultracite": "6.3.5"
  },
  "packageManager": "bun@1.3.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
